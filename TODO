Broken recipes/meta recipes: 
- Xorg-App (installs both Shared and share)
- Xorg-Driver (installs both Shared and share)
- Unknown Xorg recipe(s) causing X input to fail: 
  -> /System/Links/Shared/X11/xorg.conf.d/*.conf: need to replace /System/Kernel/Devices with /dev
- LibTool:
  Needs to be patched to get rid of the following fatal message:
    libtool: install: /bin/install -c .libs/libxcb.lai /Programs/Xorg-Lib/7.7/lib/libxcb.la
    libtool: install: error: cannot install `libxcb-composite.la' to a directory not ending in /usr/lib

bash-4.2# cd /Programs/
bash-4.2# for i in *; do [ -d $i/Current/man ] && echo $i; done
CMake
FFmpeg
Kerberos
Portmap
SYSLINUX
Wireless-Tools
Zip

dbus config files:
  -> Unknown username 'avahi', 'pulse'
  -> Unknown group 'netdev'
